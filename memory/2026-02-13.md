# 2026-02-13 — Night Shift Launch

## Chief of Staff Autonomous Mode — ACTIVATED

Gabriel wants me to operate as a true Chief of Staff — working autonomously at night to build tools that improve his workflow. We designed and launched the **Vibe Coder Night Shift** workflow in Antfarm.

### Vibe Coder Night Shift Workflow

**6-Agent Pipeline:**
1. **Observer** → Watches workflow patterns, identifies friction
2. **Planner** → Prioritizes and scopes tonight's build
3. **Architect** → Designs technical blueprint
4. **Developer** → Builds the app (vibe coding mode)
5. **Reviewer** → QA, documentation, PR prep
6. **Deployer** → Pushes to GitHub → Railway auto-deploy

**Stack:** Next.js + TypeScript + Prisma + PostgreSQL + Tailwind + Railway

**Repo:** https://github.com/garavitgabriel/rob-tools

### First Build: LinkedIn Post Manager

Gabriel needs a UI to manage his LinkedIn content pipeline:
- View pending drafts from content_pipeline.db
- See all 3 variants (educational, hot-take, behind-scenes)
- Mark posts as "posted"
- Simple analytics (posts per week)

**Status:** Workflow running, Architect step in progress (02:27 AM UTC)

### Infrastructure

- Workflow installed at: `~/.openclaw/workspace/antfarm/workflows/vibe-coder-night-shift/`
- All 6 agent cron jobs active (polling every 5 minutes)
- First run ID: `28a14070-5eb2-4a91-b464-64f8bbffec9c`

### Next Steps

1. Let first build complete (should have PR by morning)
2. Gabriel will review PR, merge to deploy
3. Schedule nightly cron job for 1:30 AM Colombia time (06:30 UTC)
4. Iterate based on what Gabriel actually uses

### Gabriel's Directives

- Wants to be surprised by what I build
- Focus on practical workflow improvements
- Proactive, not reactive
- Ship working MVPs, not perfect code
- Coordinate between all sub-agents

---

## Later Session Updates

### QMD Auto-Update Cron
- Set up hourly QMD index update cron job (`qmd-hourly-update`)
- Runs silently in isolated sessions every 60 minutes
- Keeps workspace-docs and memory collections indexed for search

### Railway Deployment Fixes (Series)

**Issue 1:** `next.config.ts` not supported on Railway
- **Fix:** Renamed to `next.config.js` with JS syntax
- **Commit:** `deb0161`

**Issue 2:** HTML comments in JSX causing build errors
- **Fix:** Converted `<!-- -->` to `{/* */}` in page.tsx files
- **Commit:** `284c652`

**Issue 3:** Build-time database queries with missing DATABASE_URL
- **Fix:** Added `export const dynamic = 'force-dynamic'` to prevent static generation
- **Commit:** `211a7b3`

**Issue 4:** `output: 'standalone'` incompatible with `next start`
- **Fix:** Removed standalone output config
- **Commit:** `44a305f`

**Issue 5:** SQLite tables not created + ephemeral storage
- **Fix:** Migrated to PostgreSQL, updated schema.prisma provider
- **Commit:** `b60f039`

**Branch:** `fix/next-config-js` (all fixes merged)
**PR:** https://github.com/garavitgabriel/rob-tools/pull/new/fix/next-config-js
**Status:** Ready for merge — includes PostgreSQL DB URL config
