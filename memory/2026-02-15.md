# 2026-02-15 - LinkedIn Workflow Recovery & DB Migration

## LinkedIn Content Workflow (Antfarm) ✅

Successfully debugged and fixed a critical failure in the `content-pipeline` workflow that was preventing Gabriel's LinkedIn posts from being generated.

### Issues Resolved
- **Infrastructure Mismatch:** Agent instructions (AGENTS.md) and workflow configs (workflow.yml) were referencing **PostgreSQL** (`psql` CLI), but the underlying database had been migrated to **SQLite**.
- **Tooling Gap:** Neither `psql` nor `sqlite3` CLI tools were installed on the host. I rewrote all 4 agents' instruction files and the workflow YAML to use `python3` with the `sqlite3` module for all DB operations.
- **Workflow Deadlock:** The Curator agent had archived content but timed out before reporting completion, causing a retry loop that found "NO_WORK". I manually completed the step to advance the pipeline.
- **Cron Delivery Failures:** The Generator cron was failing because it lacked a delivery `to` target. Added `telegram:7815864443` to the config.

### Achievements
- **Generation Recovery:** Manually processed Capture #8 (Gabriel's request for 5 AI/ML evolution posts).
- **Draft Creation:** Generated **15 LinkedIn post drafts** (3 variants x 5 topics) covering LSTMs vs APIs, Attention mechanisms, Parameters vs FLOPs, BERT vs GPT, and the end of data labeling.
- **Voice Compliance:** All drafts were reviewed by the Opus Reviewer agent and flagged with specific feedback regarding Gabriel's sharp, educational, and authentic voice.

## Git & Workspace Management
- Committed the workspace for the first time: `a7a4606` - "Fix content-pipeline: replace psql/PostgreSQL refs with python3/sqlite3".
- Resolved embedded git repository warnings (antfarm, qmd, rob-tools) by including them as tracked directories.

## Next Steps
- Gabriel needs to review the 15 drafts in the `drafts` table (status: `pending_review`).
- Verify the next `content-pipeline` run starts and completes automatically with the new Python/SQLite logic.
- Complete Google Workspace (`gog` CLI) integration once OAuth `client_secret.json` is provided.

Source: MEMORY.md, memory/2026-02-15.md
