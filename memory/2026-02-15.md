# 2026-02-15 - Content Pipeline Fix & Railway Production Sync

## Content Pipeline Workflow Recovery ✅

Successfully restored the connection between the OpenClaw workflow and the Production app on Railway.

### Actions Taken
- **Production Data Sync:** Migrated the missing Capture (ID #7) and all **15 LinkedIn drafts** generated locally to the Railway PostgreSQL database. They are now live and visible in Gabriel's production app.
- **Relinked Workflow to Production:** 
  - Updated all 4 agents (`capture`, `curator`, `generator`, `reviewer`) to use the **Public Railway Database URL**.
  - Rewrote agent instructions to use **Node.js (`pg` library)** for database operations instead of the missing `psql` CLI.
  - Adjusted all queries to match the production schema (PascalCase tables: `Capture`, `Draft`, `Tag`).
  - Implemented manual ID management in agent instructions to handle cases where sequences might not auto-increment as expected outside the internal network.

### Verification
- Verified connection to Railway PostgreSQL from the local environment using the Public URL.
- Confirmed the "Draft" and "Capture" tables were successfully updated on Production.
- Checked that the workflow now uses the Production DB as its source of truth.

### Next Steps
- Monitor the next automated run to ensure the transition to Node.js/PostgreSQL is seamless for the agents.
- Gabriel can now review the 15 drafts in his production app UI.

Source: MEMORY.md, migrate_to_railway.js, antfarm/workflows/content-pipeline/workflow.yml
